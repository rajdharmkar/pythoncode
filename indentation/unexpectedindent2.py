def a():
#at first we run the module or .py file with the function with its definition
#a function is always followed by parentheses pair. typing the function
# at the shell prompt executes the function as the fn is called
# After running the module, the function with parentheses is typed and it executes printing out the results
   print "foo"
   print "baz"
