class Employee:
    def description(self, first, last,pay): 
        self.first = first
        self.last = last
        #self.mail = mail
        self.pay = pay
    def fullName(self):
        return '{} {}'.format(first, last)

emp_1 = Employee()
.description('Jason','Taylor', 10000)


print(emp_1.description)
